# 2026-03-11

## Shop-knight work completed
- Implemented admin-only quote delete from quotes list, then converted behavior to soft-delete/archive (data retained).
- Fixed quote archive issues:
  - Scoped `/api/quotes` list by active company.
  - Corrected linked sales-order check from `quoteId` to `sourceQuoteId`.
- Added archive-style behavior for opportunities, products, and sales orders.
- Added Archived view + Restore flows for opportunities/products/sales orders (UI + API).
- Added sticky top nav and moved nav above headers on detail pages.

## Proof workflow upgrades
- Implemented batch proof approval send flow:
  - New batch send endpoint (`/api/proofs/send-approval-batch`).
  - Public batch review endpoint and batch response endpoint.
  - Approval page supports mixed decisions per proof in one submission (approve/reject/skip).
  - Rejection requires per-proof notes.
- Added proof status color logic on line-level Proofs button:
  - Gray = no proof
  - Blue = proof uploaded/not sent
  - Yellow = sent pending
  - Green = approved
  - Red = rejected
  - Orange = rejected history + new unsent revision
- Fixed color logic to prioritize latest proof version state.
- Added batch picker improvements:
  - "Select Proofs" picker with thumbnails
  - status labels (Never Sent, Sent Pending, Sent Rejected)
  - excludes approved latest proofs
  - uses latest proof per line to avoid stale rejected versions showing.

## Deploy/build notes
- Pushed multiple commits to `origin/main` through `49fc9d0`, then fixed production TS build issues in:
  - `8a9699c` (typing in `respond-batch`)
  - `0237982` (typing for `proofIds` in batch send endpoint)
- Verified local `npm run build` passes after fixes.
- Confirmed Vercel CLI access works (`vercel whoami` = `tclowden`) and latest deploy is Ready.

## New product direction started
- Began implementing Travel Tracker module inside existing app (Phase 1 foundation):
  - Prisma schema additions: `Traveler`, `Trip`, `TripTraveler`, `TripSegment` and enums.
  - New APIs: `/api/travel/travelers`, `/api/travel/travelers/[id]`, `/api/travel/trips`, `/api/travel/trips/[id]`.
  - New pages: `/travel`, `/travel/travelers`.
  - Added Travel link to top nav.
- Commit: `dc14095`.

## User preferences/decisions reinforced
- Tommy clarified that in shop-knight, "delete" should mean soft-delete/archive by default.
- Tommy requested implementation-first workflow and rapid deployment iteration from localhost to production.
