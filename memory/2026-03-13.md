# 2026-03-13

## Durable updates
- Tommy explicitly prefers UI create flows as button-triggered dedicated screens/modals (no inline always-visible create fields on list/admin pages). This was also added to MEMORY.md as a long-term preference.
- Products page UX was changed to use a dedicated Create Product screen (`/admin/products/new`) instead of inline create fields.
- Added Machines admin module:
  - Pages: `/admin/machines`, `/admin/machines/new`
  - API: `/api/admin/machines`, `/api/admin/machines/[id]`
  - DB: `Machine` model + migration `20260313115000_add_machines_admin`
- Added Product attribute builder to Create Product page so attributes can be drafted and saved during product creation.
- Built Expense Reports module (MVP):
  - Pages: `/expenses`, `/expenses/new`, `/expenses/[id]`
  - API: `/api/expenses`, `/api/expenses/[id]`, `/api/expenses/[id]/lines`, `/api/expenses/lines/[lineId]`
  - Workflow statuses: Draft/Submitted/Approved/Rejected/Reimbursed
- Added receipt upload + serving for expense lines:
  - API: `/api/expenses/lines/[lineId]/receipt`, `/api/expenses/receipts/[receiptId]`
  - DB: `ExpenseReceipt` model
- Added Amex staging queue foundation for future integration:
  - Page: `/expenses/amex`
  - API: `/api/expenses/amex`, `/api/expenses/amex/[id]/match`
  - DB: `AmexTransaction` model + status enum
- Expense report employee field changed from free text to employee lookup (`employeeUserId`) from existing users.
- Fixed `/api/users` for employee lookup issues by including SUPER_ADMIN and scoping users to active company membership.
- Built Job Workflow engine:
  - Admin template page: `/admin/job-workflows`
  - Runner page: `/jobs/workflows`
  - API: `/api/admin/job-workflows`, `/api/job-workflows/apply`, `/api/job-workflows/runs`, `/api/job-workflows/steps/[id]/complete`
  - DB models: `JobWorkflowTemplate`, `JobWorkflowStep`, `JobWorkflowRun`, `JobWorkflowStepRun`
  - Auto-handoff implemented: completing a step auto-activates next step and auto-creates assigned Task records.
- Updated Job Workflows create UX to modal/popup flow with top-level "Create Workflow" button and inner "Save Workflow" action.
- Improved workflow create error messaging to show specific duplicate-name errors and backend detail text in UI.
- Fixed prod/dev workflow create blocker caused by stale Prisma client in deploy by changing build script to `prisma generate && next build`.

## Notable deployment note
- Repeatedly encountered environment drift/migration concerns; if runtime errors persist after code deploy, production DB migration deploy is required (`npx prisma migrate deploy`).
- New standing preference: before reporting "ready to test," run an independent sub-agent validation pass and include SUPER_ADMIN-role-focused access/permission checks (not build-only validation).
